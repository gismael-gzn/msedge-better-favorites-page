export {bookmark_add, bookmark_del, bookmark_mov, bookmark_upd, bookmarks_init};

const bookmarks_init = '[BM] init'
const bookmark_add = '[BM] add';
const bookmark_del = '[BM] del';
const bookmark_mov = '[BM] mov';
const bookmark_upd = '[BM] upd';

const explorerRoute_push = '[ER] push';
const explorerRoute_up = '[ER] up';
const explorerRoute_cut = '[ER] cut';
